{
  "description": "Input that is not `{ key, value }` pairs is rejected, because feeding a plain `call` result to map@1 is a plan mistake that should surface in the dry run, not as an empty object.",
  "input": [{ "value": 1 }],
  "expectedError": "map@1 expects [{ key, value }] pairs"
}
