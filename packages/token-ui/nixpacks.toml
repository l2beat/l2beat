[phases.install]
onlyIncludeFiles = [
  "../../.nvmrc",
  "../../package.json",
  "../../pnpm-lock.yaml",
  "./package.json",
]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build:dependencies && pnpm build"]


