{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "DATABASE_URL",
        "GOOGLE_CLIENT_ID",
        "GOOGLE_CLIENT_SECRET",
        "GOOGLE_ALLOWED_DOMAINS",
        "JWT_SECRET",
        "NODE_ENV",
        "PORT",
        "SKIP_ENV_VALIDATION",
        "VERCEL_URL"
      ]
    }
  }
}
