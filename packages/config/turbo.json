{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "test": {
      "inputs": [
        "**/*.ts",
        "**/*.test.ts",
        "**/*.json",
        "**/*.jsonc",
        "../backend/**/*.json",
        "../backend/**/*.jsonc"
      ]
    }
  }
}
