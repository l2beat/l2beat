{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": [
        "**/*.ts",
        "**/*.test.ts",
        "**/*.json",
        "**/*.jsonc",
        "../backend/**/*.json",
        "../backend/**/*.jsonc"
      ]
    }
  }
}
