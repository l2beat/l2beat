import { AmountConfigEntry } from '@l2beat/shared-pure'
import { BackendProject } from '../../../backend'

export function addSharedEscrowsL1Tokens(
  _: BackendProject[],
  __: AmountConfigEntry[],
): AmountConfigEntry[] {
  return []
}
