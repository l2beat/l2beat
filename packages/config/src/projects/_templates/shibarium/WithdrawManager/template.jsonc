{
  "$schema": "../../../../../../discovery/schemas/contract.v2.schema.json",
  "description": "Contract handling users’ withdrawal finalization for tokens escrowed in DepositManager. It has blacklisting support.",
  "category": "core",
  "fields": {
    "owner": {
      "permissions": [
        {
          "type": "interact",
          "description": "manage critical config values like the exit period and the blacklist."
        }
      ]
    }
  }
}
