{
  "$schema": "../../../../../../discovery/schemas/contract.v2.schema.json",
  "displayName": "ERC20Tornado",
  "critical": true,
  "category": "core",
  "ignoreMethods": ["filledSubtrees", "roots", "zeros"],
  "fields": {
    "denomination": {
      "description": "Fixed deposit amount accepted by this pool.",
      "severity": "LOW"
    },
    "token": {
      "severity": "HIGH",
      "description": "ERC20 token accepted by this mixer instance."
    },
    "verifier": {
      "severity": "HIGH",
      "description": "Immutable zkSNARK verifier used to authorize withdrawals from the pool."
    },
    "hasher": {
      "severity": "HIGH",
      "description": "Merkle-tree hasher used for commitments and root updates."
    }
  },
  "ignoreInWatchMode": ["currentRootIndex", "getLastRoot", "nextIndex"]
}
