{
  "$schema": "../../../../../../discovery/schemas/contract.v2.schema.json",
  "description": "One-shot invariant checker for a Dual Governance upgrade. It verifies that the active and replacement deployments, timelock, committees, Aragon apps and withdrawal contracts point to the expected state.",
  "category": "gov"
}
