{
  "$schema": "../../../../../../../discovery/schemas/contract.v2.schema.json",
  "description": "A contract implementing the ITransactionFilterer interface, able to whitelist transactions based on sender addresses only. The whitelist is managed by the owner ({{owner}}).",
  "fields": {
    "owner": {
      "permissions": [
        {
          "type": "interact",
          "description": "add and remove whitelisted sender addresses."
        }
      ]
    }
  }
}
