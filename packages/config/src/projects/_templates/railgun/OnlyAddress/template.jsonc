{
  "$schema": "../../../../../../discovery/schemas/contract.v2.schema.json",
  "displayName": "OnlyAddress",
  "description": "Stateless helper that reverts unless tx.origin matches the address supplied to the call. Railgun governance can use it as a proposal-action guard.",
  "category": "shared"
}
