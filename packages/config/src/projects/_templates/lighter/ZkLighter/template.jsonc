{
  "$schema": "../../../../../../discovery/schemas/contract.v2.schema.json",
  "fields": {
    "additionalZkLighter": {
      "description": "from constructor arguments (handler does not work), upgradable by the admin.",
      "handler": { "type": "storage", "slot": 7, "returnType": "address" }
    }
  }
}
