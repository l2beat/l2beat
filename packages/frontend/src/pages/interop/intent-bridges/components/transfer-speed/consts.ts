// Default chain pair shown by IntentTransferSpeedWidget on initial render.
// Kept here so the SSR prefetch can hydrate the exact same query.
export const INTENT_TRANSFER_SPEED_DEFAULT_FROM = 'arbitrum'
export const INTENT_TRANSFER_SPEED_DEFAULT_TO = 'base'
