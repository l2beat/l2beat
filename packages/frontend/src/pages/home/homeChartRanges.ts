import type { ChartRangePredefinedOption } from '~/utils/range/range'

/** Single range for every chart on the home page. */
export const HOME_CHART_RANGE: ChartRangePredefinedOption = '1y'

/** Period suffix shown after every home chart % change ("▲ 4.2% / 1 year"). */
export const HOME_CHART_DELTA_PERIOD_LABEL = '1 year'
