import { delegatedProjectsContent } from './delegatedProjects'
import { eventsContent } from './events'
import { publicationsContent } from './publications'

export const content = {
  publications: publicationsContent,
  delegatedProjects: delegatedProjectsContent,
  events: eventsContent,
}
