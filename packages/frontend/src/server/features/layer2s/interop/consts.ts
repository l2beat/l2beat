export const INTEROP_PAIR_SEPARATOR = '::'

export const UNKNOWN_ABSTRACT_TOKEN_ID = 'unknown' as const

/** Top flows returned on the token dashboard for default chain ordering. */
export const TOKEN_DASHBOARD_TOP_FLOWS_LIMIT = 2
